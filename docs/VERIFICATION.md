# 验证记录 · v0.4.1

## 2026-09-20 Mosaic 参考版完整还原（本地源代码）

- 直接移植指定参考的七格桌面 CSS：12 列、78px 行高、14px 间距、7:5 主卡比例及五种表面。增加产品操作栏及底部同步、历史、事件区；保留参考的数字、柱条、曲线、圆环与滚动视差动画，并提供重播。
- 完整日统计新增十分钟亮屏／采集分桶、高峰、首末次和会话信息；电量使用实际上报采样。没有锁屏事件时显示“未记录”，未用剩余时间推算锁屏时长。
- Python 13 项测试通过，含新增跨午夜分桶、未闭合区间、缺口及电量采样测试；仍有 2 条依赖弃用警告。JavaScript 语法检查通过。
- 浏览器验证 320、390、768、1024、1440、1920 像素下页面与主要卡片无横向溢出；手机停用视差，时间轴和月图局部滚动。
- 日期切换、近 30 天 30 根柱、只看解锁、24 条事件展开／6 条收起、重播及减少动态效果均通过；重播可观测到 166 个运行中动画，减少动态效果后为 0。
- 模拟空数据、过期、权限缺失和 HTTP 503 的状态提示正确；刷新失败保留前次数据，恢复后正常显示。
- 目视核对桌面参考及手机长页，截图为 `output/playwright/reference-mosaic.png`、`mosaic-desktop-v2.png`、`mosaic-mobile-v2.png`。独立预览更新为相同模板、样式和脚本。
- 此次更新为本地网页和统计接口，未部署远程站点，也未重新打包 APK 或旧发行包。以下保留此前验证记录。

## 2026-09-20 Mosaic 网页调整（本地源代码）

- 桌面布局参考指定的 `screen-time-styles.html?style=mosaic`：深色背景、蓝色亮屏主卡、浅色电量与解锁卡、通栏趋势图和底部三卡。手机改为双列短统计卡与全宽同步卡。
- 数据仍来自现有公开接口，不生成不存在的电量历史或亮屏高峰；新增近 7 天亮屏小图和日期选择器使用现有每日统计。
- JavaScript 语法检查通过；Python 项目测试 10 项通过，保留原有 2 条依赖弃用警告。
- 浏览器原有流程验证 23 项通过，涵盖日期切换、筛选、展开、过期状态、权限缺失、空数据、刷新失败及恢复；额外 29 项通过，验证日期边界、周图联动、30 天自动定位、独立 HTML 预览等。
- 320、360、390、430、680、768、1024、1440 像素的 7／30 天视图均无页面或卡片横向溢出；手机 30 天图表局部横向滚动，日期点击宽度至少 44 像素。320 像素下的 23 小时 59 分、999 次解锁、100% 电量也未溢出。
- 已目视检查桌面、390 像素手机首屏及 320 像素长数值截图。证据位于 `output/playwright/mosaic-*.png`，脚本为 `verify-mosaic.js` 和 `verify-mosaic-responsive.js`。
- `workspace-preview.html` 与服务端模板、样式、交互同步，嵌入明确标注的模拟数据，可直接通过 file URL 打开。
- 本次更新本地网页源代码与预览，未发布远程服务器，未重建 APK 或已有发布压缩包。下方为前一版交付的历史验证记录。

---

日期：2026-09-20。构建环境：Windows、JDK 17、Android SDK 36。服务端在本地 Python 3.11.9 验证；不是 Rocky Linux 实机验证。本次为公开看板「亮屏之间」改版，Android 同步更新公开查看文案与版本号，采集协议不变。

## 已完成

- Android `testDebugUnitTest lintDebug assembleDebug` 成功，5 项单元测试通过。覆盖 HTTPS 地址、稳定事件 ID、上海零点及回查窗口、确认 ID 匹配、HTTP 重试分类。
- Android Lint 无错误；仍有依赖升级、同步偏好写入、KTX 使用建议及旧图标未引用等警告。没有用全局 baseline 隐藏错误。
- APK `apksigner verify --verbose` 验证成功，v2 签名有效。保留原包名和本机 Debug 签名。
- Python 3.11.9：项目测试 10 项通过，验证公开查看、无 Cookie、错误／缺失上传密钥拒绝、旧登录链接跳转、页面静态资源、空数据、重复上传、历史时间、设备冲突、时间偏差、过期清理、跨午夜、缺失区间、权限拒绝、备份恢复。配置只包含上传密钥即可运行。测试依赖有两条弃用警告，无失败。
- 依赖锁文件中的包均声明兼容 Python 3.11，实际安装了 CPython 3.11 对应依赖并运行测试。Python 3.13 也通过了初期 8 项测试。
- JavaScript 语法检查通过。
- Playwright 完成 23 项浏览器断言：统计与接口一致、近 30 天切换、日期选择、解锁筛选、展开／收起、4 种宽度乘 2 种图表范围、数据过期、权限异常、空数据、刷新失败与恢复。未上报时显示“—”，不假装零使用。
- 320、390、768、1440 像素宽度下，7／30 天图表均无整体横向溢出。已检查桌面完整页面、390×844 手机页面及 30 天图表截图。模拟数据正确显示当天 8 次解锁、1 小时 32 分亮屏、68% 电量。
- 浏览器无 JavaScript 或 CSP 错误；故障测试主动模拟一次 HTTP 503，验证提示与恢复。站点图标、脚本、样式均从本机同源加载。
- 证据：`output/playwright/verification-result.txt`、`redesign-desktop-full.png`、`redesign-mobile.png`、`redesign-mobile-month.png`、`redesign-empty.png`。测试代码 `output/playwright/verify-redesign.js` 通过 Playwright CLI 的 `run-code` 执行，不随部署包发布。
- 包采用源文件白名单，排除虚拟环境、数据库、密钥、缓存和本地预览脚本；Linux 源文件权限统一为 0644。

## 未验证／不作保证

- 没有连接 Android 手机或模拟器。未验证真实安装、UsageStats 在 HyperOS 上的事件完整性、SQLite 断电行为、升级迁移运行和后台调度。
- 单元测试覆盖上传确认决策，不替代手机离线／杀进程下的完整队列测试；请执行 `PHONE-TEST.md`。
- 未登录云服务器，未执行 Rocky Linux 的 systemd、SELinux、Nginx 或 ACME 实际部署。部署说明给出相应步骤，现场须运行 `nginx -t` 并检查服务日志。
- 不保证每 30 分钟准点同步，没有实测耗电比例，也不把未收到手机数据解释为人身异常。
- 浏览器测试为本机 HTTP 回环地址的合成数据；正式服务必须启用 HTTPS，Android 不提供跳过证书检查的选项。
